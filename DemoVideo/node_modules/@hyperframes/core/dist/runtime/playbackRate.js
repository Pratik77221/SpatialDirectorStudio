export function normalizePlaybackRate(raw) {
    return Number.isFinite(raw) && raw > 0 ? Math.max(0.1, Math.min(5, raw)) : 1;
}
/** Parse a literal numeric timing attribute without accepting trailing units or garbage. */
export function parseStrictFiniteTimingNumber(raw) {
    return parseNumeric(raw);
}
export function readElementPlaybackRate(el) {
    const authored = Number.parseFloat(el.getAttribute("data-playback-rate") ?? "");
    const raw = Number.isFinite(authored) && authored > 0
        ? authored
        : typeof HTMLMediaElement !== "undefined" && el instanceof HTMLMediaElement
            ? el.defaultPlaybackRate
            : 1;
    return normalizePlaybackRate(raw);
}
export function readMediaStart(el) {
    const parse = (raw) => {
        const value = parseStrictFiniteTimingNumber(raw);
        if (value == null)
            return null;
        return Number.isFinite(value) && value >= 0 ? value : null;
    };
    return (parse(el.getAttribute("data-playback-start")) ?? parse(el.getAttribute("data-media-start")) ?? 0);
}
export function resolveNaturalMediaTimelineDuration(el, sourceDuration) {
    return resolveNaturalMediaTimelineDurationFromValues(sourceDuration, readMediaStart(el), readElementPlaybackRate(el));
}
export function resolveNaturalMediaTimelineDurationFromValues(sourceDuration, mediaStart, playbackRate) {
    if (!Number.isFinite(sourceDuration))
        return null;
    const remaining = Math.max(0, sourceDuration - mediaStart);
    return remaining / normalizePlaybackRate(playbackRate);
}
import { parseNumeric } from "./startExpression.js";
//# sourceMappingURL=playbackRate.js.map