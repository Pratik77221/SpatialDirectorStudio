export declare const RUNTIME_BOOTSTRAP_ATTR = "data-hyperframes-preview-runtime";
/**
 * Parse a full HTML document or wrap a fragment so linkedom consistently puts
 * fragment content under document.body.
 */
export declare function parseHTMLContent(html: string): Document;
export declare function stripEmbeddedRuntimeScripts(html: string): string;
/**
 * Insert raw tag markup at the very start of `<head>`, ahead of every author
 * script (inline or external). Falls back to just before `<body>`, then to the
 * top of the document, for fragments that carry neither.
 */
export declare function injectTagsAtHeadStart(html: string, tags: string): string;
export declare function injectScriptsAtHeadStart(html: string, scripts: readonly string[]): string;
export declare function injectScriptsIntoHtml(html: string, headScripts: readonly string[], bodyScripts: readonly string[], stripEmbeddedRuntime?: boolean): string;
//# sourceMappingURL=htmlDocument.d.ts.map