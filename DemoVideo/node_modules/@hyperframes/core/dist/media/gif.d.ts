export interface AnimatedGifMetadata {
    width: number;
    height: number;
    frameCount: number;
    delaysCentiseconds: number[];
    durationSeconds: number;
    /** Netscape loop count. 0 means infinite; null means no loop extension was present. */
    loopCount: number | null;
    animated: boolean;
}
export declare function parseAnimatedGifMetadata(bytes: Uint8Array): AnimatedGifMetadata | null;
//# sourceMappingURL=gif.d.ts.map