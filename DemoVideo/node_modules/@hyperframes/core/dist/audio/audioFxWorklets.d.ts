/** True once the processors are usable on this context. */
export declare function audioFxWorkletsReady(ctx: BaseAudioContext): boolean;
/**
 * Register the processors on a context. Idempotent per context, since
 * addModule throws if the same processor name is registered twice.
 */
export declare function ensureAudioFxWorklets(ctx: BaseAudioContext): Promise<void>;
//# sourceMappingURL=audioFxWorklets.d.ts.map