/**
 * "Freeze" = write asset bytes to local disk permanently so renders never
 * re-fetch from figma (design spec §5) — not Object.freeze.
 */
export declare const MAX_FREEZE_BYTES: number;
export declare function exceedsFreezeCap(byteLength: number): boolean;
export declare function freezeBytes(bytes: Uint8Array, destPath: string): number;
/**
 * Only figma-owned hosts may be frozen from a URL — render/CDN responses
 * come from figma.com subdomains or figma's S3 buckets. Blocks SSRF via a
 * crafted manifest/config URL (metadata endpoints, internal services).
 */
export declare function isAllowedFreezeUrl(url: string): boolean;
export declare function freezeUrl(url: string, destPath: string): Promise<number>;
export declare function freezeLocalFile(srcPath: string, destPath: string): void;
//# sourceMappingURL=freeze.d.ts.map